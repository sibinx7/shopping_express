/**
 * @method
 * @name checkProjectCompleteness
 * @summary Check project status
 * @example
 * 	checkProjectCompleteness(project) => incomplete|complete|submitted
 * @description Check for Project status
 * @param {object} project Project information
 * @return {string} status Project status incomplete|complete|submitted
 *
 */

export const checkProjectCompleteness = (project) => {
	let status = "incomplete";




};